import { ThemeNamePipe } from './theme-name.pipe';

describe('ThemeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
